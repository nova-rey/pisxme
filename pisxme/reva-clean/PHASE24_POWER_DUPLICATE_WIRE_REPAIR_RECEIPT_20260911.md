# Phase 24 duplicate POWER_INPUT wire repair receipt

Date: 2026-09-11

`POWER_INPUT.kicad_sch` contained two serialized copies of four identical
3-mm wires. A disposable complete native project removed exactly one copy of
each geometry, leaving one owned wire per connection. Native KiCad 10.0.5
ERC changed from 489 warnings / 0 errors to **485 warnings / 0 errors**.

The probe is `.phase24_power_duplicate_wire_probe/` and includes child
sheets, local libraries, and library tables. The four geometries were
`(70,41.25)-(67,41.25)`, `(70,38.75)-(67,38.75)`,
`(70,81.25)-(67,81.25)`, and `(70,78.75)-(67,78.75)`.

The exported native netlist `(nets)` section is identical to the pre-repair
export after normalizing autogenerated net codes: no missing, extra, or
changed net names or `(ref,pin)` node sets. The hierarchy structure audit
passes, with no native errors or hierarchy findings introduced.

The four duplicate records were removed from authoritative
`POWER_INPUT.kicad_sch`. This is a source repair, not a severity change or
waiver. Evidence is retained in `PHASE24_POWER_DUPLICATE_PROMOTED-erc.rpt`
and `PHASE24_POWER_DUPLICATE_PROMOTED.net`.

**CLOSED — duplicate POWER_INPUT wire serialization root cause.**
