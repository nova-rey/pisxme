# RTL9210B-CG isolated bring-up fixture — 2026-09-07

Status: **ISOLATED DESIGN-EVIDENCE FIXTURE; NOT PRODUCTION CAD**

Generated by `phase24_make_rtl9210b_bringup_fixture.py` and audited by
`phase24_rtl9210b_bringup_fixture_audit.py`. It is separate from
`STORAGE.kicad_sch` and the clean acreage PCB.

## Captured authority

- corrected RTL9210B-CG qualification SMD QFN-68 footprint, including exposed
  chip pad 69 on `GND`;
- one M-key 2280 socket boundary;
- lane-0 mapping: RTL 68/67 → M.2 49/47 and RTL 64/65 ← M.2 43/41;
- PCIe REFCLK 61/62 → contacts 55/53;
- PERST/CLKREQ → contacts 50/52;
- PEDET pin 8 ↔ M.2 contact 69;
- USB2/USB3, 5 V/3.3 V/1.1 V, RSET, ISOLATEB, clock, SPI flash, reset,
  decoupling, pull-up, SSD-power, and programming/test net boundaries;
- accessible SPI, reset, UART, and ground test pads.

The fixture deliberately does not invent a Realtek application-circuit value
or firmware image. Support components are boundary footprints and named nets;
their final placement/value/layout still requires the authorized/current
application circuit and a traceable prototype part.

## Native result

```text
phase24_rtl9210b_bringup_fixture_audit.py                    PASS
phase24_rtl9210b_bringup_fixture_audit.py --negative-control PASS (fails as intended)
native KiCad DRC                                              0 violations
native KiCad unconnected items                                56 (expected)
```

The 56 unconnected items are intentional at this stage: the fixture captures
the support-net/pad boundary but does not claim routed high-speed copper or a
completed power/return implementation. No shorts, crossings, footprint-rule
violations, or library-resolution warnings remain in the saved DRC report.
The open items are not silently waived; they are the explicit next fixture
gate before any hardware order or production promotion.

## Promotion boundary

This closes the isolated design-evidence step for the corrected package and
mapping. It does not close virgin-chip programming, firmware provenance,
application-circuit authority, SSD 3.3-V inrush/thermal proof, or hardware
SATA/NVMe mode validation. Path A remains intact and the fixture is not used
as a reason to alter the clean board.
