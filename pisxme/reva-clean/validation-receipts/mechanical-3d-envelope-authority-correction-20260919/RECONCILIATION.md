# Mechanical 3D envelope authority correction

This reconciliation is issued under MPA decision `validation-receipts/mpa-m2-service-corridor-20260919/MPA_DECISION.json`.

The prior signed mechanical contract's J3 identity (`JAE SM3ZS067U410ABR1000`, B-key) is stale for the selected Path-A implementation. The governing J3 identity is TE `1-2199230-4`, M-key, fixed at `(220,165)`, top side. This correction supersedes only the stale J3 provenance field; it does not replace the current J3, change pad/net ownership, or change the full 2280 envelope.

The full populated 2280 envelope remains `[220,149]–[300,171]` at `(260,160)` with right-edge insertion. Connector mating volume is represented separately from the card-body no-go envelope. J8 is populated and moves to `(205,140)`, 0 degrees, top side. Exact stack height, retention, and prototype mating remain validation requirements; no DRC waiver is granted.
