# Native script-author dispatch stall

The bounded native `pcbnew` script-author dispatch ended without a candidate,
producer receipt, or live process. The preceding Light API probe had already
verified KiCad 10.0.6 availability, so this is a workflow/implementation
execution stall, not evidence against the Molex architecture or MPA placement.
Canonical CAD was untouched. The package is parked on Unblocker for one
capability-level route; the rejected post-MPA candidate remains the only
physical validation failure.
