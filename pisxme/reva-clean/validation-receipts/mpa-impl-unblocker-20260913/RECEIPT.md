# MPA implementation unblocker — 2026-09-13

The bounded MPA revision producer is classified as an implementation-method failure, not a structural contradiction. Native DRC on the qualified candidate path reported 782 violations, 31 shorts, 25 crossings, 499 unconnected items, and 156 track-width violations; the candidate used invalid 0.10 mm geometry and coordinate-scripted/shared-via routes. Its claimed KiCad version also conflicted with the required 10.0.6 context.

The next permitted method is one bounded implementation attempt using qualified KiCad 10.0.6, exact mechanically derived pad centers, immediate native collision checks, and incremental routing. No guessed launches, shared vias, invalid widths, synthetic edges, or speculative variant campaign are allowed.

Endpoint correction: the prior `AUTO_PEDET` pair using `U14.2` is stale. Current PCB evidence identifies `U14.2` as `MODE_IN`; `AUTO_PEDET` is on `J3.69` / `J8.2`. Reconcile the control-net contract before routing. This correction supersedes that one stale endpoint and does not alter other Path-A pairs.

Independent lanes remain actionable: ERC/source hygiene, schematic/pad coverage, library/pin mapping, regulator overlays, SI/layer/return analysis, provenance, mechanical/DFM, and hostile review.
