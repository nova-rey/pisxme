# Native heavy single-branch experiment — runtime result — 2026-09-13

The qualified `pisxme-kicad-heavy:v1` worker successfully launched `pcbnew` (PID 10) against the exact MPA-placed baseline. The bounded attempt produced no native route artifact, no per-segment collision record, and no changed PCB. The container lacked `xprop`/`xwininfo` for window inspection, and the attempt was stopped without changing canonical CAD.

This proves heavy-image process launch, but not successful interactive routing. It is runtime tooling insufficiency for the discriminating experiment, not corridor infeasibility. No further route variants are authorized without a supported native interaction path.
