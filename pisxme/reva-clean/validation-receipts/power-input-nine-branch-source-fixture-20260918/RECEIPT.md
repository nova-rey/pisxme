# Nine-branch source fixture receipt

This isolated source fixture was generated from committed base `ccfaaf51d02d22a652b21bf630120100e84484dd` after the Light worker exposed a missing project symbol-library context for `PiSXMeRevAClean:POWER_INPUT_HEADER`.

The bounded tool-context correction uses `Connector_Generic:Conn_02x03_Odd_Even` as the generation symbol while preserving value `0039300060` and the authorized project footprint name. It contains J5/J6/J9, F1-F9, and B1-B9 positive/return labels. Native netlist export completed with KiCad 10.0.6 and no stderr. This is a source-contract fixture, not an integrated CAD candidate; PCB implementation, source-to-J1 connectivity, DRC, path budget, and thermal/DFM validation remain open.

Hashes are retained in `fixture.sha256`.
