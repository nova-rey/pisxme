# Current integrated power/return census

Board: current `PHASE24_FCU_POWER_GND_WIDTH_NORMALIZED.kicad_pcb` at
campaign head `831da328` (metadata-only descendants do not alter PCB).

The native serialization census reports 7 pads and zero segments/vias for
both `12V_IN_B` and `FUSED_12V_B`; `12V_PROTECTED` has 151 pads, 6 segments,
and one via. The result confirms branch-B delivery remains physically open.
This is read-only evidence; it does not close power acceptance.
