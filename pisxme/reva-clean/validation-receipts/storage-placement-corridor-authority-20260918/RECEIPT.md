# Storage placement/corridor authority receipt — 2026-09-18

Package `P24-STORAGE-PLACEMENT-CORRIDOR-AUTHORITY` returned
`BINDING_DECISION`. The authoritative packet is `DECISION.md` and
`DECISION.json`; hashes are in `SHA256SUMS`.

The decision resolves the direct U11/L10 courtyard contradiction by moving only
L10 to `(143.00,127.80)`, top side, rotation `0°`. U11 remains `(140,135)` and
Y10 remains `(138.2,126.4)`, both top side at `0°`. Path A, the fixed M-key
socket, protected-bus structures, and validated high-speed corridors remain
unchanged.

This packet authorizes a producer placement/corridor implementation. It does
not claim route connectivity, DRC, mechanical release, or Phase 24 closure.
